<UserControl x:Class="Library.LibraryMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Height="35" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Библиотека" FontSize="18" FontWeight="Black" Margin="15,5,0,0"/>
        <Button Grid.Column="1" Margin="0,5,15,5" Click="SearchButton_Click">
            Поиск
        </Button>
        <Button Grid.Column="2" Margin="0,5,15,5">
            Мои книги
        </Button>
        <Button Grid.Column="3" Margin="0,5,15,5">
            Аккаунт
        </Button>
    </Grid>
</UserControl>
